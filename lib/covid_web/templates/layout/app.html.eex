<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tracking Corona - COVID-19 Data Visualization</title>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
  </head>

  <body class="bg-gray-100 text-gray-900">
    <div class="bg-gray-100 min-h-screen">
      <nav x-data="{ open: false }" class="bg-gray-800 shadow-sm">
        <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="hidden py-3 sm:flex">
                <%= link "Confirmed", to: Routes.page_path(@conn, :confirmed), class: "#{active(@conn, Routes.page_path(@conn, :confirmed), "bg-gray-900")} rounded-md font-medium py-2 px-3 text-white text-sm focus:outline-none focus:text-white focus:bg-gray-700" %>
                <%= link "Recovered", to: Routes.page_path(@conn, :recovered), class: "#{active(@conn, Routes.page_path(@conn, :recovered), "bg-gray-900")} rounded-md font-medium py-2 px-3 text-white text-sm focus:outline-none focus:text-white focus:bg-gray-700 ml-2" %>
                <%= link "Deaths", to: Routes.page_path(@conn, :deaths), class: "#{active(@conn, Routes.page_path(@conn, :deaths), "bg-gray-900")} rounded-md font-medium py-2 px-3 text-white text-sm focus:outline-none focus:text-white focus:bg-gray-700 ml-2" %>
              </div>
            </div>

            <div class="flex items-center -mr-2 sm:hidden">
              <button @click="open = !open"
                      class="rounded-md inline-flex items-center justify-center p-2 text-gray-400 transition ease-in-out duration-150 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:bg-gray-100 focus:text-gray-500">
                <svg class="h-6 w-6" stroke="currentColor" fill="none" viewBox="0 0 24 24">
                  <path :class="{'hidden': open, 'inline-flex': !open }" class="inline-flex"
                        stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M4 6h16M4 12h16M4 18h16" />
                  <path :class="{'hidden': !open, 'inline-flex': open }" class="hidden"
                        stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M6 18L18 6M6 6l12 12" />

                </svg>
              </button>
            </div>

          </div>
        </div>

        <div :class="{'block': open, 'hidden': !open}" class="hidden sm:hidden">
          <div class="px-2 pt-2 pb-3 sm:px-3">
            <%= link "Confirmed", to: Routes.page_path(@conn, :confirmed), class: "#{active(@conn, Routes.page_path(@conn, :confirmed), "bg-gray-900")} rounded-md block font-medium mt-1 py-2 px-3 text-gray-300 text-base hover:text-white hover:bg-gray-700 focus:outline-none focus:text-white focus:bg-gray-700"%>
            <%= link "Recovered", to: Routes.page_path(@conn, :recovered), class: "#{active(@conn, Routes.page_path(@conn, :recovered), "bg-gray-900")} rounded-md block font-medium mt-1 py-2 px-3 text-gray-300 text-base hover:text-white hover:bg-gray-700 focus:outline-none focus:text-white focus:bg-gray-700"%>
            <%= link "Deaths", to: Routes.page_path(@conn, :deaths), class: "#{active(@conn, Routes.page_path(@conn, :deaths), "bg-gray-900")} rounded-md block font-medium mt-1 py-2 px-3 text-gray-300 text-base hover:text-white hover:bg-gray-700 focus:outline-none focus:text-white focus:bg-gray-700"%>
          </div>
        </div>
      </nav>

      <main>
        <div class="mx-auto sm:px-6 lg:px-8 max-w-7xl">
          <div class="py-8">
            <%= render @view_module, @view_template, assigns %>
          </div>
        </div>
      </main>
    </div>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.0.1/dist/alpine.js" defer></script>
  </body>

</html>
